// template-common: 공통 유틸리티 라이브러리
// spring 라벨이 spring-boot-starter, validation, json, logstash-logback-encoder 처리
dependencies {
  implementation(rootProject.libs.com.googlecode.libphonenumber)
  implementation(rootProject.libs.com.nimbus.jose.jwt)
}
